# Handoff, 2026-09-04

For a fresh Claude Code session in the clai checkout. Read `CLAUDE.md` first, then this file, then `docs/decisions.md`.

## Where things stand
- v0.1 builds, typechecks with zero errors, and passes 183 tests (core, store, 14 connectors). `npm run build && npm test`.
- Verified on real data: `clai scan` ingests 250+ Claude Code transcripts in under four seconds; August 2026 API-equivalent usage $3,864.54, identical to the cent with ccusage (independent tool). The Claude Code connector dedups by (message id, request id) and takes the field-wise maximum across streamed lines (see decisions log).
- Team mode verified end to end: bootstrap admin token, member token, `clai sync` of 1,738 events with paths stripped, member-scoped views, admin-only endpoints.
- Dashboard: 8 pages, light/dark, mock/empty/team modes, checked against the real backend.
- Provider API connectors (Anthropic Admin, Claude Code Analytics, Enterprise Analytics, OpenAI Admin, OpenRouter, Cursor Admin, GitHub Copilot) are fixture-tested against documented shapes and cross-checked against `research/`; they have NOT been run against live accounts yet.
- Founding plan page (market case, plan, status, decisions): https://claude.ai/code/artifact/f004cf26-01d8-4a1a-95df-1ac9fd3b2b6e (private artifact owned by the founder).

## Decisions taken by the founder (2026-09-04)
- npm org `claii` (bare `clai` was taken); packages `@claii/*`, binary `clai`. Renamed throughout.
- GitHub `vcgetter22/clai`, currently private; remote `origin` is configured, nothing committed or pushed yet.
- Pricing approved: Free / Plus $7 / Team $5 per seat (5 minimum) / Business by quote.
- Partial open source approved: this repo (core, store, connectors, CLI, dashboard, self-hosted server) under MIT; the hosted service and Business features stay in a separate private repo.
- Founder is on Claude Max 5x. Design rules in `docs/design-principles.md`; Claude Design prompt in `docs/claude-design-prompt.md`.

## Immediate next steps
1. Initial commit and push (founder to confirm), CI runs on GitHub Actions (`.github/workflows/ci.yml`).
2. Pre-publish checklist in `docs/release.md`: versions aligned at 0.1.0, `npm publish` in dependency order under `@claii`, test `npx @claii/cli@latest --version` on Node 22.13 and 24.
3. Make the repo public at the moment of the first npm publish (same day), after a final sweep for personal data (`grep -rIn "fmbp\|/Users/" .` returned nothing in tracked files; the `research/` notes contain only public sources).
4. Run the API connectors against live accounts (a Claude Console org with an Admin key, an OpenAI org with an Admin key, a Cursor Business team, a GitHub org with Copilot) and fix shapes marked UNVERIFIED in code comments.
5. Landing page and calculator from the Claude Design output; launch post draft in `docs/launch-post.md`.
6. Hosted service (Postgres adapter behind `EventStore`, Stripe, alerts): new private repo `clai-cloud`; do not provision Supabase/Vercel without the founder's explicit go-ahead.

## Known gaps and caveats
- `clai ask` needs `ANTHROPIC_API_KEY` or `ant auth login`; untested against a live key in this build.
- claude.ai export imports counts only unless `--estimate --model` is passed (the export records neither tokens nor model).
- OpenAI GPT-5.4+ long-context tier threshold is unpublished; priced at the base tier.
- Dashboard bundle is 641 KB (React + Recharts); code-splitting is a later polish item.
- Claude Code OTLP receiver, Bedrock/Azure/Vertex/M365 connectors, browser extension: roadmap (`docs/roadmap.md`).

## How the work was verified (repeatable)
```bash
npm install && npm run build && npm test && npm run typecheck:all
CLAI_HOME=/tmp/clai-check node apps/cli/bin/clai.js scan && CLAI_HOME=/tmp/clai-check node apps/cli/bin/clai.js doctor
npx -y ccusage@latest monthly   # independent cross-check of the Claude Code numbers
```
